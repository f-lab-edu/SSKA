spring:
  config:
    activate:
      on-profile: test
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:Skka-for-test;MODE=MYSQL;
    username: skka
    password:
  flyway:
    enabled: true
    encoding: UTF-8
    user: skka
    password: